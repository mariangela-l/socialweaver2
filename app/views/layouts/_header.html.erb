<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "socialweaver", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to "Create a Project", new_project_path %><span class="sr-only">(current)</span></a></li>
        <li><%= link_to "Explore", projects_path %></li>
        <li><a href="#">About</a></li>   
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in?  %>
        <li><p>Hello, <%= current_user.email %></p></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Create a Project", new_project_path %></li>
            <li><%= link_to "Account", "account_path" %></li>
            <li><%= link_to "Settings", "#" %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
          <% else %>
        <li><%= link_to "Sign Up", new_user_registration_path %></li>
        <li><%= link_to "Log In", new_user_session_path %></li>
        <% end %>

       
      </ul>

      <form class="navbar-form navbar-right">
        <div class="form-group">
          <%= form_tag projects_path, method: :get, class: 'navbar-form navbar-right' do %>
          <%= text_field_tag :search, params[:search], class: 'form-control', placeholder: 'search for projects' %>
          <%= submit_tag "Search", class: 'btn btn-default' %>
          <% end %>
      </form>
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>